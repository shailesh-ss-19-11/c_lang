#include<stdio.h>
#include<string.h>
int main(){

    char str1[100] ="have a nice day";
    char str2[100] ="have a nice day";
    char fullstr[100];
    int value,length;

    // strcpy() //copy the string 
    // strcmp() //compare the string
    // strlen() //find the length of string
    // strcat() //join the string
    // strrev() //reverse the string
    // strupr() //uppercase
    // strlwr() //lowercase
            
    // strcpy(str2,str1);  //have a nice day, str2 is destination 
    // printf("%s\n",str2);

    // value = strcmp(str1,str2);
    // printf("%d\n",value);

    // length = strlen(str1);
    // printf("%d",length);

    // printf("%s",strcat(str1,str2));

    // printf("%s",strlwr(str1));
}