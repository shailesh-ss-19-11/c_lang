#include <stdio.h>
#include <string.h>
int main(){

    // string is collection of characters 
    // collection of characters = array of chars

    char sentence[100];
    int len;
    printf("enter the string :");    // 
    fgets(sentence,100,stdin);  //gets 
    printf("%s\n",sentence);
    len =strlen(sentence);
    printf("%d\n",len);


// string provides 2 functions gets puts
}

// input string from user and find the vowel in string 

