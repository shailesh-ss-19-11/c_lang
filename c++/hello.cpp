// procedural programming language 

// object oriented programming 
// class  -- user define data type , 
        // class consist data members and member functions also visibility modes
        // in OOP we can call function as methods 
        
// encapsulation    -- binding the data and functions together
// inheritance      --  accessing the properties of parent class to child class 
                    // 5 types of inheritance 
                    // single, multiple ,multilevel, hybrid ,hierarchical 
// data abstraction -- hiding the complexity of the data , showing essential part of data ignore the unuseful data
// object           -- variable of a class, instance of a class,
                    // use for access the data members and member function using . operator
// constructor      -- constructor is a special member function which initialize the object,
                    // constructor called only once, 
// destructor       -- destructor the object 
// polymorphism     -- the word consist many forms, its a hierarchi of classes, relate with inheritance
// visibility modes -- access modifiers, public, private, protected 
// function overloading -- same functions with addition of parameters or different paramenters 
// constructor overloading --same name with addition of parameters or different paramenters 

